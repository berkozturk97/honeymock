import { Box } from '@chakra-ui/react'
import Step2 from '../../components/LoginSteps/Step2'

function LoginStep2() {
   return(     <Box w={1000} backgroundColor='#fff' h={'auto'} p={10} m={(0, 'auto')} mt={10}>
    <Step2 />
  </Box>)

}

export default LoginStep2;